---
// About.astro
import { Icon } from "astro-icon/components";
---

<section
  id="about"
  class="px-4 sm:px-8 lg:px-24 text-white font-roboto-mono py-20 relative overflow-hidden"
>
  <!-- Background subtle grid pattern -->
  <div class="absolute inset-0 bg-grid-pattern opacity-5 z-0"></div>

  <main class="relative z-10 max-w-4xl mx-auto">
    <div class="flex flex-col lg:flex-row gap-8 lg:gap-12">
      <!-- Left column: Image and short intro -->
      <div class="lg:w-1/3 flex flex-col items-center lg:items-start">
        <div class="relative">
          <!-- Highlight circle behind image -->
          <div
            class="absolute inset-0 bg-purple-800/20 rounded-full blur-md transform scale-110"
          >
          </div>

          <!-- Main image -->
          <img
            src="/me.jpeg"
            alt="Angel - Desarrollador Backend"
            class="rounded-full w-40 h-40 sm:w-48 sm:h-48 object-cover shadow-lg border-2 border-purple-400/70 relative z-10"
          />

          <!-- Purple dot accent -->
          <div
            class="absolute -bottom-2 -right-2 w-6 h-6 bg-purple-500 rounded-full border-2 border-zinc-900 z-20"
          >
          </div>
        </div>

        <!-- Name with typing effect -->
        <h3
          class="text-3xl sm:text-4xl font-bold mt-6 mb-2 typewriter relative"
        >
          <span class="relative z-10">Angel</span>
        </h3>

        <!-- Social links -->
        <div class="flex gap-3 mt-2">
          <a
            href="#"
            class="text-zinc-400 hover:text-purple-400 transition-colors"
          >
            <span class="sr-only">GitHub</span>
            <Icon name="mdi:github" class="text-2xl" />
          </a>
          <a
            href="#"
            class="text-zinc-400 hover:text-purple-400 transition-colors"
          >
            <span class="sr-only">LinkedIn</span>
            <Icon name="mdi:linkedin" class="text-2xl" />
          </a>
          <a
            href="#"
            class="text-zinc-400 hover:text-purple-400 transition-colors"
          >
            <span class="sr-only">Spotify</span>
            <Icon name="mdi:spotify" class="text-2xl" />
          </a>
        </div>
      </div>

      <!-- Right column: Bio and details -->
      <div class="lg:w-2/3 flex flex-col lg:justify-center space-y-6">
        <div>
          <div
            class="inline-block px-3 py-1 bg-purple-900/30 border border-purple-700/30 rounded-md text-purple-300 text-sm mb-3"
          >
            <span class="font-mono">{"< about_me >"}</span>
          </div>

          <h2 class="text-2xl sm:text-3xl font-bold mb-4">
            Desarrollador Backend
          </h2>

          <p class="text-base sm:text-lg text-zinc-300 leading-relaxed">
            Soy un <span class="text-purple-400 font-semibold"
              >desarrollador backend</span
            >
            apasionado por las nuevas tecnologías y siempre en busca de aprender
            más y crear soluciones eficientes. Me especializo en construir APIs robustas
            y escalables con tecnologías modernas.
          </p>
        </div>

        <!-- Tech stack -->
        <div class="pt-4">
          <p class="text-sm uppercase tracking-wider text-zinc-500 mb-3">
            Actual Stack
          </p>
          <div class="flex flex-wrap gap-2">
            <span
              class="px-3 py-1 bg-zinc-800/50 rounded-md text-sm border border-zinc-700/50 hover:border-purple-500/50 transition-colors"
              >Express</span
            >
            <span
              class="px-3 py-1 bg-zinc-800/50 rounded-md text-sm border border-zinc-700/50 hover:border-purple-500/50 transition-colors"
              >TypeScript</span
            >
            <span
              class="px-3 py-1 bg-zinc-800/50 rounded-md text-sm border border-zinc-700/50 hover:border-purple-500/50 transition-colors"
              >PostgresSQL</span
            >
          </div>
        </div>
      </div>
    </div>
  </main>
</section>

<style>
  /* Sutble grid pattern */
  .bg-grid-pattern {
    background-image: linear-gradient(
        to right,
        rgba(255, 255, 255, 0.05) 1px,
        transparent 1px
      ),
      linear-gradient(to bottom, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
    background-size: 20px 20px;
  }

  /* Typewriter effect */
  .typewriter::after {
    content: "|";
    display: inline-block;
    margin-left: 0.1em;
    animation: blink 1s step-end infinite;
  }

  @keyframes blink {
    from,
    to {
      opacity: 1;
    }
    50% {
      opacity: 0;
    }
  }

  /* Highlight text selection */
  ::selection {
    background-color: rgba(168, 85, 247, 0.3);
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .lg:w-1/3 {
      width: 100%;
    }
    .lg:w-2/3 {
      width: 100%;
    }
    .lg:flex-row {
      flex-direction: column;
    }
    .lg:gap-12 {
      gap: 8px;
    }
    .text-3xl {
      font-size: 2.25rem;
    }
    .text-4xl {
      font-size: 2.5rem;
    }
    .text-2xl {
      font-size: 1.5rem;
    }
    .sm:text-3xl {
      font-size: 2rem;
    }
    .sm:text-lg {
      font-size: 1.125rem;
    }
  }
</style>
